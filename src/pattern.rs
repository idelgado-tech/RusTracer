use crate::color::Color;

pub struct Pattern {
    pub color_a :Color,
    pub color_b :Color,
    pub fonction :Color

    // fn get_color1() -> Color;
    // fn set_color1(color: &Color) -> ();
    // fn get_color2() -> Color;
    // fn set_color2(color: &Color) -> ();
    // fn color_at_point(point: Tuple) -> Color;
}

